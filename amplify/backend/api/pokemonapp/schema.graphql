type Captured
  @model
  @auth(rules: [{ allow: public, operations: [create, read, update] }]) {
  id: ID!
  title: String!
  captured: [Pokemon] @hasMany
}

type Pokemon
  @model
  @auth(rules: [{ allow: public, operations: [create, read, update] }]) {
  id: ID!
  name: String!
  src: String!
}
